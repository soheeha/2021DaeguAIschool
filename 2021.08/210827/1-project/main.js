var $favoritesIcon = $('.favorites_icon');

$favoritesIcon.click(function() {

	/*
	if($(this).hasClass('on')) {
		$(this).removeClass('on');
	} else {
		$(this).addClass('on');
	}
	*/

	$(this).toggleClass('on');

})
